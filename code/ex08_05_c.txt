// nf()를 사용한 숫자 자리수 맞추기

function setup() {
  let num1 = 12;
  let a = nf(num1, 3);		// 정수인 경우
  let b = nf(num1, 5);		// 정수인 경우
  console.log(a);
  console.log(b);

  let num2 = 10.5;
  let c = nf(num2, 0, 2);	// 실수인 경우
  let d = nf(num2, 4, 2);	// 실수인 경우
  console.log(c);
  console.log(d);
}